source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-feed", "~> 0.17"

# Lock file: only used if you run `bundle install` locally.
# GitHub Pages builds in its own sandbox using github-pages versions.
